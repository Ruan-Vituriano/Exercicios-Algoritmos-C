/*A matriz gerada deve armazenar, em cada
posição, a quantidade de grãos que deve ser armazenada em cada posição do
tabuleiro. Após gerar e preencher a matriz, o programa deve imprimi-la para o usuário.*/

#include <stdio.h>
